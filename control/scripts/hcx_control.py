import subprocess
import time


def hcx_start():
    # print('kurec')
    # bash_command = "sudo hcxdumptool -i wlx000f00775385 -o home.cap --enable_status=3"
    # process = subprocess.Popen(bash_command.split(), stdout=subprocess.PIPE)
    # output, error = process.communicate(timeout=10)
    #
    # # time.sleep(3)
    # # process.sendcontrol('c')
    # # process.close()

    print("lmao")

    return 0

hcx_start()